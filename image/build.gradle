apply plugin: 'com.android.library'

android {
    compileSdkVersion 28
    defaultConfig {
        minSdkVersion 21
        targetSdkVersion 28
        consumerProguardFiles 'glide.pro'
    }
}

dependencies {
    api "com.github.bumptech.glide:glide:4.8.0"
}
